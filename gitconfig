[alias]
    dup = commit
    wciep = apply
    dodej = add
    
    targej - pull
    piznij = push
    fajrant = !sh -c 'git pull --rebase && git push'
    
    curig = revert
    
    kiery = blame
    
    hasie = stash
    hasiewciep = stash apply
    
    kajjesmajster = !sh -c 'git shortlog HEAD..origin/master'
    
